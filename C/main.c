#include "matrix_op.h"
#include <stdio.h>
#include <stdlib.h>
#include "tests.h"
#include "macros.h"
// cmd + shift + p -> edit configuration

int main() {
  #ifdef RUN_TEST
  test_results();
  return 0;
  #else
  return 0;
  #endif
}
